// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   // Asegura que se aplique a rutas protegidas
//   // matcher: ["/dashboard/:path*"],
//    reactStrictMode: true,
// };

// export default nextConfig;

// next.config.js

const nextConfig = {
  // Asegura que se aplique a rutas protegidas
  // matcher: ["/dashboard/:path*"],
  reactStrictMode: true,
};

module.exports = nextConfig;
